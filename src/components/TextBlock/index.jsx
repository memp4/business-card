import React from 'react';
import './index.scss';

const TextBlock = ({ children }) => {
    return (
        <div className="text-block">
            {children}
        </div>
    );
};

export default TextBlock;



